{
  "name": "sample-node-project",
  "version": "1.0.0",
  "description": "Sample Node.js Project",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "mkdir -p dist && cp -R package.json dist/ && echo 'Build completed'",
    "lint": "echo 'Linting passed'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Saraswathi",
  "license": "ISC",
  "dependencies": {
    "express":"latest",
    "ejs":"latest"
  }
}
